import OnboardingForm from "./OnboardingForm";

export const metadata = {
  title: "Store Onboarding — ePAiD",
  description:
    "Onboard your store with ePaid — complete business, store, KYC, and payment details in a few simple steps.",
};

export default function OnboardingPage() {
  return <OnboardingForm />;
}
